import React from "react";

import RegisterForm from "./RegisterForm";

function RegisterMain(params) {
    return(
        <main>
            <RegisterForm/>
        </main>
    );
}

export default RegisterMain;